---
"%{auth_type}":
  - ceph::authentication_type
mon_defaults:
  - ceph::mons::defaults
key_defaults:
  - ceph::keys::defaults
"%{ensure}":
  - ceph::ensure
  - ceph::repo::ensure
fsid:
  - ceph::fsid
mon_host:
  - ceph::mon_host
packages:
  - ceph::params::packages
public_network:
  - ceph::public_network
release:
  - ceph::repo::release
