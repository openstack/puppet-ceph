---
:backends:
  - data_mapper
:hierarchy:
  - "ensure/%{ensure}"
  - user
  - "osfamily/%{osfamily}"
  - "auth_type/%{auth_type}"
  - "hostname/%{hostname}"
  - "scenario/%{scenario}"
  - common
:data_mapper:
  :datadir: /etc/puppet/data/data_mappings
