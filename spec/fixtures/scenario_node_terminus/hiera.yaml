---
:backends:
  - data_mapper
:hierarchy:
  - user
  - "osfamily/%{osfamily}"
  - "ensure/%{ensure}"
  - "auth_type/%{auth_type}"
  - "hostname/%{hostname}"
  - "scenario/%{scenario}"
  - common
:data_mapper:
  :datadir: /etc/puppet/data/data_mappings
