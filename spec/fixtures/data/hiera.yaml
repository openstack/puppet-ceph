---
:backends:
  - yaml
:hierarchy:
  - "ensure/%{ensure}"
  - user
  - "osfamily/%{osfamily}"
  - "auth_type/%{auth_type}"
  - "scenario/%{scenario}"
  - "scenario/%{scenario}/%{role}"
  - "scenario/%{auth_type}/%{role}"
  - common
  - class_groups
:yaml:
  :datadir: /etc/puppet/data/hiera_data
